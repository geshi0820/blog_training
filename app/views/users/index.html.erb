<%= stylesheet_link_tag "profile" %>
<h1><%= current_user.username %>のタイムライン</h1>
<div id="container">
	<div id="main">
		<% @articles.each do |article| %>
		<div id="contents">
			<div class="title"><%= image_tag article.user.user_image %></div>
			<h2 class="title"><%= article.title %></h2>
			<% if current_user.favorites.find_by(article_id: article)  %>
			<h2 class="favorite"><%= link_to "♥",favorite_path(current_user.favorites.find_by(article_id: article)), method: :delete %></h2>
			<% else %>
			<h2 class="favorite"><%= link_to "♡",article_favorites_path(article),method: :post %></h2>
			<% end -%>
			<h3 class="favorite"><%= link_to "show",article_path(article)%></h3>
			<br><br><br><br><br>
			<div class="item" ><%= image_tag article.image_url%></div>
			<div class="article"><%= article.article %></div>
		</div>
		<% end -%>
	</div>
</div>
<a href="<%= users_path %>">TOPに戻る。</a>
</div>
