    <style>
        #mycanvas {
            border: 10px solid #999;
            cursor: crosshair;
        }
        .thumbnail {
            border: 2px solid #999;
            margin-right: 5px;
        }
    </style>
    <p>
        <select id="penColor">
            <option value="black">黒</option>
            <option value="red">赤</option>
            <option value="blue">青</option>
            <option value="white">白</option>
            <option value="green">緑</option>
            <option value="yellow">黄</option>
        </select>
        <select id="penWidth">
            <option value="1">細</option>
            <option value="3">中</option>
            <option value="5">太</option>
            <option value="15">極太</option>
        </select>
        <input type="button" id="erase" value="消去">
        <input type="button" id="save" value="登録">
    </p>
    <canvas width="200" height="200" id="mycanvas">
        Canvasに対応したブラウザを用意してください。
    </canvas>
    
    <div id="gallery"></div> 
    <input type="button" id="push" value="ほぞん">
    <br>
    <br>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
    <script>
        $(function() {
            var canvas = document.getElementById('mycanvas');
            if (!canvas || !canvas.getContext) return false;
            var ctx = canvas.getContext('2d');
            var startX,
            startY,
            x,
            y,
            borderWidth = 10,
            isDrawing = false;
            $('#mycanvas').mousedown(function(e) {
                isDrawing = true;
                startX = e.pageX - $(this).offset().left - borderWidth;
                startY = e.pageY - $(this).offset().top - borderWidth;
            })
            .mousemove(function(e) {
                if (!isDrawing) return;
                x = e.pageX - $(this).offset().left - borderWidth;
                y = e.pageY - $(this).offset().top - borderWidth;
                ctx.beginPath();
                ctx.moveTo(startX, startY);
                ctx.lineTo(x, y);
                ctx.stroke();
                startX = x;
                startY = y;
            })
            .mouseup(function() {
                isDrawing = false;
            })
            .mouseleave(function() {
                isDrawing = false;
            });
            $('#penColor').change(function() {
                ctx.strokeStyle = $(this).val();
            });
            //ペンの色のタブ
            $('#penWidth').change(function() {
                ctx.lineWidth = $(this).val();
            });
            //ペンのサイズをタブにする
            $('#erase').click(function() {
                //id=erase のボタンをクリックする
                if (!confirm('本当に消去しますか？')) return;
                ctx.clearRect(0, 0, canvas.width, canvas.height);
            });
            $('#save').click(function() {
                var img = $('<img>').attr({
                    width: 200,
                    height:200,
                    src: canvas.toDataURL()
                });
                //saveのボタンをクリックすると、canvasをURLにする。
                var link = $('<a>').attr({
                    href: canvas.toDataURL().replace('image/jpg', 'application/octet-stream'),
                });
                $('#gallery').append(link.append(img));
                ctx.clearRect(0, 0, canvas.width, canvas.height);
            });
            $("#push").on("click", function () {
                var form = $("#gallery");
                var canvas = document.getElementById("gallery");
                $("#item_image").val(""); //画像データを二重に送信するのを防ぐ
                var e = document.getElementById("item_remote_image_url")
                e.textContent=canvas
            });
        });
    </script>








