<div class="container" style="padding:20px 0">
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Title</th>
				<th>Time</th>
				<th>Show</th>
				<th>Edit</th>
				<th>Drop</th>
			</tr>
		</thead>
<% @article_id.each do |f| %>
		<tbody>	
			<tr>
				<td><%= @articles.find(f).title %></td>
				<td><%= @articles.find(f).created_at %></td>
				<td><%= link_to "show", article_path(f),method: :get-%></td>
				<td><%= link_to "edit", edit_article_path(f),method: :get -%></td>
				<td><%= link_to "delete", article_path(f), method: :delete -%></td>
			</tr>
		</tbody>
		<% end -%>
	</table>
</div>
